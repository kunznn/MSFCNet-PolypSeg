# Checkpoints

Large model weights are not stored directly in this repository.

The final full MFCNet checkpoint used in the manuscript corresponds to:

```text
a3_polypmfc_msm_bcf_kvasircvc_jointtrain_best.pth
```

Place the checkpoint file in this folder before running evaluation or visualization scripts.
